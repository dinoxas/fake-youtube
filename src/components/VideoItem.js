import React, { Fragment } from "react";
import { Paper, Typography } from "@material-ui/core";

const VideoItem = () => {
  return (
    <Fragment>
      <Paper>
        <Typography variant="h4">Video item</Typography>
      </Paper>
    </Fragment>
  );
};

export default VideoItem;
